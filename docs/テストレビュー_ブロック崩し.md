# テストレビュー: ペンギンブロックブレイク

| 項目 | 内容 |
|------|------|
| ドキュメント種別 | テストレビュー |
| プロジェクト名 | GameSite |
| ゲーム名 | ペンギンブロックブレイク |
| 作成日 | 2026-04-06 |
| 対象ブランチ | `feature/block-breaker-game` |
| レビュー結果 | 合格 |

---

## 1. 追加テスト

| ファイル | 確認内容 |
|----------|----------|
| `web/src/games/breakout/domain/__tests__/score-calculator.test.js` | 基本点、コンボボーナス、異常値 |
| `web/src/games/breakout/domain/__tests__/stage-state.test.js` | ブロック生成、破壊済み更新、残数 |
| `web/src/games/breakout/domain/__tests__/game-session.test.js` | 状態遷移、得点、コンボ、残機、ステージ進行 |
| `web/src/games/breakout/application/__tests__/start-game.test.js` | ステージ読み込み、初期セッション生成、空配列エラー |

---

## 2. 実行結果

### Vitest

```text
Command: npm test -- --run
Result: 19 files passed / 111 tests passed
Duration: 1.18s
```

### Vite Build

```text
Command: npm run build
Result: success
Modules: 71 modules transformed
Build time: 686ms
```

---

## 3. 実行メモ

- PowerShell の PATH に `node` が無かったため、`C:\Program Files\nodejs` を一時的に PATH へ追加して実行した
- サンドボックス内では Vite の config 読み込み時に `spawn EPERM` が発生したため、承認付きでサンドボックス外実行に切り替えた
- 既存テストも含めて全件成功した

---

## 4. 判定

- ドメイン・アプリケーションの主要ルールは自動テストで確認済み
- ルーター統合と動的 import はビルドで確認済み
- 残作業は本番 FTP デプロイ承認と、必要に応じたブラウザ上の操作感確認
